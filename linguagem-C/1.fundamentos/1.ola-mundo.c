#include <stdio.h>                  // Biblioteca de procedimentos de leitura e escrita
int main(void)                      // Rotina principal (main) do programa executável
{
    printf("Olá mundo\n");          // Escrever Olá mundo e pular linha (\n)
    return 0;                       // Finalizar execução com sucesso (0 de erro)
}