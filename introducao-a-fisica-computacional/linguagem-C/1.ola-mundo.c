//  Incluir header de procedimentos padrão de leitura e escrita
#include <stdio.h>

int main(void)                      // Rotina principal (main) do programa executável
{
    printf(                         // Efetuar a escrita padrão de
        "Olá mundo\n"               // 'Olá mundo' e adicionar nova linha (\n)
    );
    return 0;                       // Retornar da execução com sucesso (erro 0)
}